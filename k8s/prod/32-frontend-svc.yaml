apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: prod
  labels: { app: frontend }
spec:
  type: ClusterIP
  selector:
    app: frontend
    color: blue   # << flip to 'green' to promote
  ports:
  - port: 8080
    targetPort: 8080
